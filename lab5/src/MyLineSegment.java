
/** Класс, описывающий произвольный отрезок */
public class MyLineSegment extends MyGraphicPrimitive2D {

    private int secondX;
    private int secondY;

    public MyLineSegment(int x1, int y1, int x2, int y2) {
        if () this(x1, y1, x2, y2);
    }

    private MyLineSegment(int x, int y, int width, int height) {
        super(x, y, width, height);

    }
}
